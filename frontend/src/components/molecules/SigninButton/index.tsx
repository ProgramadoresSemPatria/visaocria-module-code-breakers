import Button from 'src/components/atoms/Button';

const SigninButton = () => {
  return (
    <Button className="py-[6.5px] px-[12px] bg-[#4814b0] text-[#e0e0e0] rounded hover:bg-[#6036d1] transition-colors ease-linear transform duration-200">
      Sign in
    </Button>
  );
};

export default SigninButton;
